import { StatusIcon } from './status-icon';

describe('StatusIcon', () => {
  it('should create an instance', () => {
    const directive = new StatusIcon();
    expect(directive).toBeTruthy();
  });
});
