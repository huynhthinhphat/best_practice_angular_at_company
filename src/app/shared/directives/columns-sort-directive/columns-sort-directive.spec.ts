import { ColumnsSortDirective } from './columns-sort-directive';

describe('ColumnsSortDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnsSortDirective();
    expect(directive).toBeTruthy();
  });
});
